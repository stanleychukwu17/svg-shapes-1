function findSequence (obj) {
    let jamil = []
    let numStages = obj.length;

    ajoke = true;

    for (var i = 0; i < numStages; i++) {
        console.log(obj[i]);
        jamil.
    }

    while (ajoke) {
        LoopSt()
        ajoke = false;
    }
}
  
let james = findSequence([0, 12, 2, 10, 6, 14, 1, 9, 5, 13, 3, 11, 7, 15]);
// console.log(james)